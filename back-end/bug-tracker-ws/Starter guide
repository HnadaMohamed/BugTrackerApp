Guide to start : 
	-Create project.
	-Add dependency.
	-Add application context.
	-Create Entities, DTO, Repositories.
	-Adding JPA annotations to Entities.
	-Create User(Service,Repo,Controller).
	-Add Authentication Filter and generate token (Spring Security + Jwt).
	
	
	
Generate war and deploy in docker container to start : 
	- generate war 
	- navigate to project folder and run : docker-compose up --build
	- url localhost/ warName 
	